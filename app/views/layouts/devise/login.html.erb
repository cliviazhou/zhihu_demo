<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8"/>
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">

  <title>Login </title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="ui middle aligned center aligned grid" id="login">
  <div class="column" id="col">
    <h2 class="ui teal image header">
      <div class="content">
        用户登陆
      </div>
    </h2>
    <%= form_for(resource, as: resource_name, :html => { class:'ui large form', }, url: session_path(resource_name)) do |f| %>
          <div class="ui stacked segment">
            <div class="field">
              <div class="ui left icon input">
                <i class="user icon"></i>
                <%= f.email_field :email, autofocus: true, :placeholder => 'E-mail address' %>
              </div>
            </div>
            <div class="field">
              <div class="ui left icon input">
                <i class="lock icon"></i>
                <%= f.password_field :password, autocomplete: 'off', :placeholder => 'Password' %>
              </div>
            </div>
            <%= f.submit '登陆', :class => 'ui fluid large teal submit button' %>
          </div>

    <% end %>
    <div class="ui message">
      注册新用户? <a href="/users/sign_up">Sign Up</a>
    </div>
  </div>
</div>

</body>
</html>