<nav class="ui teal inverted fixed menu">
  <div class="item"><i class="home icon"></i><%= link_to 'Home', '/' %> </div>
  <div class="item"><i class="lab icon"></i><%= link_to 'Answers', '/answers' %> </div>
  <div class="item"><i class="user icon"></i> <%= link_to 'People', '/person' %></div>
  <div class="item">Projects</div>
  <div class="item">News</div>
  <div class="right menu">
    <% if current_user %>
        <div class="item"><i class="mail outline icon"></i> <%= current_user.email %></div>
        <div class="item">
          <button class="ui inverted blue button"><%= link_to 'Signout', destroy_user_session_path, :method => :delete %>
          </button>
        </div>
    <% else %>
        <div class="item">
          <button class="ui inverted blue button"> <%= link_to 'Signin', '/users/sign_in' %></button>
        </div>
    <% end %>
  </div>
</nav>
